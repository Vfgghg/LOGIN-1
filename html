@{
    Layout = null;
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <title>TIME HR Login</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Your custom CSS -->
    <link rel="stylesheet" href="~/Content/Contact.css" />
</head>
<body>
    <div class="container-fluid vh-100 d-flex align-items-center justify-content-center">
        <div class="row w-100 shadow-lg" style="max-width:1200px;">

            <!-- Left Info Panel -->
            <div class="col-lg-6 col-md-6 col-sm-12 info-panel d-flex flex-column justify-content-center px-lg-5 px-md-4 px-3 py-5 border-end border-3 border-primary">
                <h1 class="mb-4" style="font-weight: 900; font-size:3rem;">
                    <span class="welcome">Welcome</span> to <span class="brand">Time<span class="highlight1">HR</span></span>
                </h1>
                <p class="text-white-50" style="max-width:400px; font-size:1.1rem; line-height:1.6;">
                    TIME HR simplifies managing your workforce, attendance, and payroll with smart, intuitive tools designed for your organization’s success.
                </p>
            </div>

            <!-- Right Login Panel -->
            <div class="col-lg-6 col-md-6 col-sm-12 login-panel d-flex flex-column justify-content-center px-lg-5 px-md-4 px-3 py-5" style="background-color:#1a2228;">
                <div class="logo mb-4">
                    <img src="~/imgs/logo.png" alt="TIME HR Logo" class="img-fluid" style="max-width:140px;" />
                </div>
                <h2 class="mb-4" style="color:#307fad; font-weight:700;">Sign In to Your Account</h2>

                <form method="post" action="/Account/Login" novalidate>
                    <input type="text" name="orgCode" placeholder="Organization Code" required class="form-control mb-3 custom-input" />
                    <input type="text" name="username" placeholder="Username" required class="form-control mb-3 custom-input" />
                    <input type="password" name="password" placeholder="Password" required class="form-control mb-3 custom-input" />

                    <div class="d-flex justify-content-between align-items-center mb-4 options">
                        <label class="form-check-label d-flex align-items-center gap-2 text-white">
                            <input type="checkbox" name="remember" class="form-check-input custom-checkbox" /> Remember me
                        </label>
                        <div class="links">
                            <a href="#" class="text-decoration-none link-secondary me-3">Forgot Password?</a>
                            <a href="#" class="text-decoration-none link-secondary">Forgot ORGCODE?</a>
                        </div>
                    </div>

                    <button type="submit" class="btn btn-ca5840 w-100 fw-bold py-3 mb-3">Sign In</button>

                    <p class="signup-text text-white-50 text-center">Don't have an account? <strong><a href="#" class="text-primary text-decoration-none">Sign Up</a></strong></p>
                </form>
            </div>

        </div>
    </div>

    <!-- Bootstrap Bundle JS (Popper + Bootstrap JS) -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
